# ============================================
# Study Buddy - Consolidated Environment Variables
# ============================================
# Copy this file to .env and fill in your values
# This file is used by both frontend and backend

# ============================================
# DATABASE CONFIGURATION
# ============================================
# For SQLite (local development):
#   Option 1: Leave empty (default) - Uses backend/core/study_buddy.db
#   Option 2: Set to sqlite:///path/to/database.db
#     Examples:
#     - sqlite:///backend/core/study_buddy.db (relative path)
#     - sqlite:////absolute/path/to/db.db (absolute path, note 4 slashes)
#     - sqlite:///:memory: (in-memory, for testing)
# For PostgreSQL (production/Vercel):
#   Set to postgresql://user:password@host:port/database
DATABASE_URL=

# ============================================
# JWT SECRET KEY
# ============================================
# Generate a secure key with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-super-secret-key-change-in-production

# ============================================
# EMAIL CONFIGURATION
# ============================================
# For Gmail, you'll need to use an App Password, not your regular password
# Go to: Google Account > Security > 2-Step Verification > App passwords
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password-here
MAIL_FROM=your-email@gmail.com
MAIL_PORT=587
MAIL_SERVER=smtp.gmail.com
MAIL_FROM_NAME=Study Buddy

# For other email providers:
# Outlook/Hotmail: MAIL_SERVER=smtp-mail.outlook.com
# Yahoo: MAIL_SERVER=smtp.mail.yahoo.com

# ============================================
# CLOUDINARY CONFIGURATION (Image Uploads)
# ============================================
# Sign up at https://cloudinary.com and get these values from your dashboard
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# ============================================
# FRONTEND CONFIGURATION
# ============================================
# Vite requires VITE_ prefix for environment variables
# For local development: Use http://localhost:8000
# For production: Leave empty to use relative /api URL
VITE_API_BASE_URL=http://localhost:8000

# ============================================
# CORS CONFIGURATION (Optional)
# ============================================
# Comma-separated list of allowed origins
# Leave empty or set to * to allow all origins
# Example: https://your-app.vercel.app,https://www.your-app.com
CORS_ORIGINS=
